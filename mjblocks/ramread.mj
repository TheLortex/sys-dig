filler<n>()=(a:[n]) where
	if n=0 then
		a = []
	else
		a = filler<n-1>().0
	end if
end where


ram_reader<addr_size , word_size>(read_addr:[addr_size])=(sortie:[word_size]) where 
sortie=ram<addr_size , word_size >(read_addr , 0 , read_addr , filler<word_size>())
end where
